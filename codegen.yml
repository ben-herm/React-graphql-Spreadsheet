overwrite: true
schema: "https://capital-mastodon-23.hasura.app/v1/graphql"
documents: "graphql/**/*.graphql"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
